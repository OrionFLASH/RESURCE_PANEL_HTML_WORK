# Задача агенту: распределение сумм (UI + демо)

**Репозиторий:** `RESURCE_PANEL_HTML_WORK`  
**Не трогать:** SPOD_PROM / SPOD_PARCE_LOAD.

## Статус (2.4.6, 2026-08-10)

- Дерево фильтров ТБ→ГОСБ (серые кликабельны; ГОСБ→ТБ; ТБ→все ГОСБ); кластеры отдельно.
- Таблица интервалов: абсолюты + `% всех` + `% в ТБ/ГОСБ/кластере` + «Итого».
- Подписи/hover/экспорт/ползунки — как в 2.4.5.
- Настройки: `#app-config` в `sum-distribution.html`.

## Commit/push

Только `sum-distribution.html`, тесты, docs. Без `index.html`, без `samples/*.csv`.
