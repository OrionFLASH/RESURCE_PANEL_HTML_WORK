# Задача агенту: распределение сумм (UI + демо)

**Репозиторий:** `RESURCE_PANEL_HTML_WORK`  
**Не трогать:** SPOD_PROM / SPOD_PARCE_LOAD.

## Статус (2.4.8, 2026-08-11)

- Вкладки: **Один период** / **Сравнение периодов** (текущий + прошлый).
- Виды графика: **гистограмма** / **линия** (площадь убрана).
- Разрезы: вся выборка / ТБ / кластер.
- Сравнение: наложение | **наложение с перекрытием** | рядом | друг под другом; общие границы и фильтры.
- Вертикальный layout на всю ширину; один график растянут; два — друг под другом.
- Фильтры: фиксированный порядок; серые = не выбраны; цвет блока all/partial/none; одна пара **Выбрать всё / Снять всё**.
- Вставка CSV убрана — панель статистики файла.
- Демо: `tools/build_sum_demo_csv_20k.py`, `tools/build_sum_demo_csv_21k.py` (~21k / 1900 ТН).
- Настройки: `#app-config` в `sum-distribution.html`.

## Commit/push

Только `sum-distribution.html`, `tools/build_sum_demo_csv_21k.py`, тесты, docs.  
Без `index.html`, без `samples/*.csv`, без `Новая папка/`.
