# Задача агенту: распределение сумм (UI + демо)

**Репозиторий:** `RESURCE_PANEL_HTML_WORK`  
**Не трогать:** SPOD_PROM / SPOD_PARCE_LOAD.

## Статус (2.4.7, 2026-08-11)

- Режим границ только ползунки под графиком (выбор count/width убран).
- Кнопки:
  - **«Равные интервалы»** — равная ширина по шкале сумм.
  - **«Равно по числу ТН»** — равные доли участников (N → N+1 интервалов).
  - **«Лесенка по числу ТН»** — убывающие доли K…1 (первый больше всех).
- Бейдж номера группы не на макс-бегунке.
- Дерево фильтров ТБ→ГОСБ; таблица с `% всех` / `% в блоке`; подписи/hover/экспорт.
- Настройки: `#app-config` в `sum-distribution.html`.

## Commit/push

Только `sum-distribution.html`, тесты, docs. Без `index.html`, без `samples/*.csv`.
