# Задача агенту: распределение сумм (UI + демо)

**Репозиторий (единственный нужный):**  
`/Users/orionflash/Documents/MyProject/RESURCE_PANEL_HTML_WORK`

**Не трогать:** `SPOD_PROM` / `SPOD_PARCE_LOAD` — это другой проект (каталог параметров Excel). График и демо-CSV к нему не относятся.

---

## Контекст

Страница: `sum-distribution.html` — единый HTML (гистограмма сумм по ТН). Настройки — JSON `#app-config` в начале файла (отдельный `config.json` не используется).

## Статус (2.4.5, 2026-08-10)

Сделано:

1. Sticky-график + ползунки (по умолчанию `custom`, 2 границы, бейджи групп); целые границы; суммы с разрядами.
2. Подписи ТН на столбиках (цвет по серии); адаптивные подписи оси X; в режиме «Значения» — имя ТБ + стата.
3. Hover по столбику — статистика интервала/серии.
4. Таблица интервалов: абсолюты ТБ/ГОСБ/кластер/ТН + «Итого»; при разрезе — блоки по значениям.
5. При выборе разреза «Группы на графике» по умолчанию = «Значения разреза».
6. Экспорт CSV/TXT; демо `tools/build_sum_demo_csv_20k.py`; тесты `node src/Tests/test_histogram_bins.js`.

## Что сделать агенту сейчас

1. Открыть **RESURCE_PANEL_HTML_WORK**, не SPOD_PROM.
2. Прогнать тесты: `node src/Tests/test_histogram_bins.js`.
3. **Внимание:** изменение `index.html` — не из задачи гистограммы; не коммитить вместе.
4. Commit/push только sum-distribution + docs (без `samples/*.csv`, без `index.html`).

## Файлы к коммиту (ожидаемый набор)

- `sum-distribution.html` (в т.ч. `#app-config`)
- `README.md`, `ROADMAP.md`, `Docs/TASK_SUM_DISTRIBUTION_UI.md`
- тесты/tools при изменениях

## Не делать

- Не переносить эти правки в SPOD_PROM.
- Не добавлять CSV из `samples/` в git.
