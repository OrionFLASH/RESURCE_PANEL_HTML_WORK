# Ресурсная панель (Resource Panel)

## Формулировка задачи и техническое задание

Проект представляет собой веб-приложение на чистом HTML/CSS/JavaScript для создания ресурсной панели с быстрым доступом к различным внутренним сервисам и инструментам организации. Панель обеспечивает удобную навигацию по рабочим ресурсам, группируя их по категориям, и включает в себя набор утилит для административных задач.

### Основные требования:
- Создание интуитивно понятного интерфейса с группировкой ресурсов
- Адаптивный дизайн для различных размеров экранов
- Проверка доступности ссылок в реальном времени
- Модальные окна для различных административных утилит
- Экспорт конфигурации в JSON-файл с датой и временем
- Динамическая адаптация размеров кнопок для заполнения всего блока группы

## Подробное описание решения

Проект реализован как одностраничное веб-приложение (SPA) на чистом HTML, CSS и JavaScript без использования внешних библиотек. Основные компоненты:

### Структура интерфейса:
1. **Динамический контейнер с группами ресурсов** - сетка из карточек, загружаемых из встроенного JSON-конфигурации. Каждая группа представляет категорию сервисов с карточками-ссылками
2. **Группа «Администрирование»** — модалки и копирование DevTools-скриптов:
   - Декодер permission (расшифровка HEX-прав)
   - Проверка роли сотрудника (ПСИ)
   - Кнопки копирования DevTools-скриптов (поле `scriptFile` в JSON): сначала из `JS/`, при ошибке — из встроенной копии в `index.html`
   - Экспорт конфигурации ссылок в JSON
3. **Адаптивная сетка** - автоматическое изменение количества колонок в зависимости от размера экрана (4 колонки по умолчанию, адаптация до 1 колонки на мобильных устройствах)
4. **Встроенная конфигурация** - все данные о группах и ссылках хранятся в JSON-строке внутри HTML, что позволяет работать без сервера
5. **Адаптивная система размеров** - кнопки автоматически заполняют весь блок группы, шрифт адаптируется под размер кнопок, автоматическое переключение между 1 и 2 колонками

### Функциональные возможности:
- **Динамическая загрузка контента** - группы и карточки загружаются из встроенного JSON-конфига
- **Автоматическая проверка доступности ссылок** при загрузке страницы и каждые 2 минуты
- **Визуальная индикация недоступных ресурсов** (серый цвет для недоступных ссылок)
- **Копирование DevTools-скриптов** — клик по карточке с `scriptFile`: загрузка из `JS/` (fetch или sync XHR), запасной вариант — встроенные блоки в `index.html`; на `file://` копирование синхронное в обработчике клика (сохраняется user activation); toast с источником и числом символов
- **Экспорт конфигурации в JSON-файл** с датой и временем в имени файла
- **Копирование учетных данных в буфер обмена** по клику на определенные ссылки (с поддержкой SberBrowser)
- **Унифицированные модальные окна** с единой структурой, размерами и стилями
- **Подсказка по ссылке при наведении** — при наведении на карточку показывается полупрозрачное окно (стиль Liquid Glass) с полным URL, доменом, путём и параметрами запроса; для модальных карточек (`isModal: true` или `url: "#"`) выводится сообщение «Модальное окно или ссылка не задана»
- **Копирование ссылки по Ctrl/Cmd + клик** — комбинация Ctrl+клик (Windows/Linux) или Cmd+клик (macOS) по карточке копирует полный URL в буфер обмена без перехода по ссылке, с краткой анимацией подтверждения
- **Адаптивный дизайн** для различных размеров экранов (десктоп, планшет, мобильный)
- **Умная адаптация размеров** - кнопки заполняют весь блок группы, шрифт автоматически подстраивается

## Полный список переменных и функций

### CSS Переменные (Custom Properties)

```css
--glass-bg-gradient: linear-gradient(135deg, #f5f7fa 0%, #e8ecf1 50%, #d4e0ed 100%)
--glass-white: rgba(255, 255, 255, 0.7)
--glass-white-light: rgba(255, 255, 255, 0.6)
--glass-white-border: rgba(255, 255, 255, 0.5)
--glass-text: #1d1d1f
--glass-text-secondary: #6e6e73
--glass-blue: #007AFF
--glass-blue-light: rgba(0, 122, 255, 0.15)
--glass-purple: #5856D6
--glass-red: #FF3B30
--glass-green: #34C759
```

**Назначение:** Централизованное управление цветовой схемой приложения в стиле Liquid Glass (iOS 26).

### JavaScript Функции

#### `adjustTopActions()`
**Назначение:** Адаптирует расположение кнопок верхнего блока в зависимости от количества групп на странице. Если групп меньше 3, кнопки располагаются вертикально, иначе горизонтально.

**Пример использования:**
```javascript
adjustTopActions(); // Вызывается автоматически при загрузке и изменении размера окна
```

#### `setStatus(card, isAvailable)`
**Параметры:**
- `card` (HTMLElement) - элемент карточки ссылки
- `isAvailable` (boolean) - флаг доступности ресурса

**Назначение:** Устанавливает визуальный статус доступности для карточки ссылки (зеленый/серый).

**Пример использования:**
```javascript
setStatus(cardElement, true);  // Помечает как доступную
setStatus(cardElement, false); // Помечает как недоступную
```

#### `checkCard(card)`
**Параметры:**
- `card` (HTMLElement) - элемент карточки с ссылкой

**Назначение:** Асинхронно проверяет доступность URL через HEAD-запрос и обновляет статус карточки.

**Пример использования:**
```javascript
checkCard(document.querySelector('.ios-card')); // Проверяет одну карточку
```

#### `checkAllCards()`
**Назначение:** Проверяет доступность всех ссылок на странице и анимирует прогресс-бар.

**Пример использования:**
```javascript
checkAllCards(); // Вызывается автоматически при загрузке и каждые 2 минуты
```

#### `copyScriptFromFile(cardElement, scriptFile)`
**Параметры:**
- `cardElement` (HTMLElement) — карточка-кнопка
- `scriptFile` (string) — имя файла в `JS/` (поле `scriptFile` в JSON)

**Назначение:** Загружает текст скрипта и копирует в буфер. На `file://` — синхронно (файл → встроенная копия). На http(s) — async fetch с fallback. Показывает toast с источником и длиной текста.

#### `loadScriptText(scriptFile)` / `loadScriptTextSyncForCopy(scriptFile)`
**Назначение:** Загрузка текста скрипта. Async-вариант для http(s); sync-вариант для `file://` (XHR + встроенные блоки).

#### `copyToClipboardSync(text)`
**Назначение:** Синхронное копирование через событие `copy` и `execCommand` — надёжно при `file://` и SberBrowser.

#### `initEmbeddedScriptsStore()` / `getEmbeddedScriptText(scriptFile)`
**Назначение:** Чтение встроенных копий из `<script type="text/plain" class="embedded-script">` между маркерами `EMBEDDED_SCRIPTS_START/END`.

#### `generateScriptSber(type)`
**Параметры:**
- `type` (string) - тип окружения: 'alpha' или 'sigma'

**Назначение:** Генерирует JavaScript-скрипт для DevTools для выгрузки данных лидеров из SalesHeroes по указанным Tournament ID.

**Пример использования:**
```javascript
generateScriptSber('alpha');  // Генерирует скрипт для Alpha-окружения
generateScriptSber('sigma');  // Генерирует скрипт для Sigma-окружения
```

#### `generateNewsScript(env)`
**Параметры:**
- `env` (string) - окружение: 'alpha' или 'sigma'

**Назначение:** Генерирует скрипт для выгрузки деталей новостей по их кодам.

**Пример использования:**
```javascript
generateNewsScript('alpha'); // Генерирует скрипт для Alpha
```

#### `buildRewardScript({codes, baseUrl, cookie})`
**Параметры:**
- `codes` (Array<string>) - массив кодов наград
- `baseUrl` (string) - базовый URL API
- `cookie` (string) - cookie для аутентификации

**Назначение:** Строит скрипт для выгрузки данных по наградам (rewards) с пагинацией.

**Пример использования:**
```javascript
buildRewardScript({
  codes: ['r_01_2025-0_10-1_2'],
  baseUrl: 'https://efs-our-business-prom.omega.sbrf.ru/bo/rmkib.gamification/api/v1/badges/',
  cookie: document.cookie
});
```

#### `formatTabNumbers(input)`
**Параметры:**
- `input` (string) - строка с табельными номерами, разделенными любыми символами

**Возвращает:** Array<string> - массив отформатированных табельных номеров (8 цифр с ведущими нулями)

**Назначение:** Парсит и форматирует табельные номера из произвольного текстового ввода.

**Пример использования:**
```javascript
formatTabNumbers('12345, 54321; 13579'); 
// Возвращает: ['00012345', '00054321', '00013579']
```

#### `generateProfileScript(baseUrl)`
**Параметры:**
- `baseUrl` (string) - базовый URL API (Alpha или Sigma)

**Назначение:** Генерирует скрипт для выгрузки профилей сотрудников по табельным номерам.

**Пример использования:**
```javascript
generateProfileScript('https://efs-our-business-prom.omega.sbrf.ru');
```

#### `loadAndRenderConfig()`
**Назначение:** Загружает конфигурацию из встроенного JSON и рендерит все группы и карточки на странице. Сохраняет конфигурацию в глобальную переменную `globalConfig` для последующего экспорта.

**Пример использования:**
```javascript
loadAndRenderConfig(); // Вызывается автоматически при загрузке страницы
```

#### `renderGroups(groups)`
**Параметры:**
- `groups` (Array<Object>) - массив объектов групп с карточками

**Назначение:** Рендерит HTML-элементы для всех групп ресурсов на основе конфигурации.

#### `createGroupElement(group)`
**Параметры:**
- `group` (Object) - объект группы с полями: id, name, code, cards, isAdmin

**Возвращает:** HTMLElement - DOM-элемент группы

**Назначение:** Создает DOM-элемент для одной группы ресурсов с заголовком и контейнером для карточек.

#### `createCardElement(card)`
**Параметры:**
- `card` (Object) - объект карточки с полями: title, url, id, copyCode, isModal

**Возвращает:** HTMLElement - DOM-элемент карточки

**Назначение:** Создает DOM-элемент для одной карточки-ссылки с обработкой модальных окон и внешних ссылок.

#### `initDynamicEventHandlers()`
**Назначение:** Инициализирует обработчики событий для динамически созданных элементов (карточки с копированием кода, модальные окна, кнопка экспорта).

#### `adaptGroupsToFit()`
**Назначение:** Адаптирует размеры кнопок в группах для заполнения всего доступного пространства. Автоматически переключается между 1 и 2 колонками в зависимости от количества кнопок. Вычисляет оптимальную высоту кнопок и размер шрифта на основе доступного пространства. Уменьшает размеры при необходимости, чтобы избежать прокрутки.

**Логика работы:**
1. Вычисляет доступную высоту контейнера
2. Определяет количество колонок (1 или 2) на основе количества кнопок
3. Вычисляет оптимальную высоту кнопки для заполнения всего блока
4. Вычисляет оптимальный размер шрифта на основе высоты кнопки
5. При необходимости уменьшает размеры до минимума
6. Применяет вычисленные размеры ко всем кнопкам

#### `exportConfigToJSON()`
**Назначение:** Экспортирует текущую конфигурацию в JSON-файл с датой и временем в имени файла. Формат имени: `resource_panel_config_YYYYMMDD_HHMMSS.json`.

**Пример использования:**
```javascript
exportConfigToJSON(); // Вызывается при клике на кнопку "Экспорт ссылок"
```

#### `ModalManager`
**Назначение:** Универсальный менеджер для управления модальными окнами с поддержкой закрытия по клику вне окна, по Escape и фокуса.

**Методы:**
- `register(id, options)` - регистрирует модальное окно
- `open(id)` - открывает модальное окно
- `close(id)` - закрывает модальное окно

**Пример использования:**
```javascript
ModalManager.register('permission-modal');
ModalManager.open('permission-modal');
ModalManager.close('permission-modal');
```

#### `copyToClipboard(text, buttonElement)`
**Параметры:**
- `text` (string) - текст для копирования
- `buttonElement` (HTMLElement, optional) - элемент кнопки для отображения обратной связи

**Назначение:** Копирует текст в буфер обмена с поддержкой современного API и fallback для SberBrowser.

**Пример использования:**
```javascript
copyToClipboard('текст для копирования');
```

#### `Logger`
**Назначение:** Утилита для логирования с уровнями INFO и DEBUG. Может быть отключена через `CONFIG.LOGGING_ENABLED`.

**Методы:**
- `info(message, data)` - логирование информационных сообщений
- `debug(message, data)` - логирование отладочной информации
- `error(message, data)` - логирование ошибок

**Пример использования:**
```javascript
Logger.info('Страница загружена');
Logger.debug('Данные конфигурации', config);
```

#### `safeQuerySelector(selector, context)`
**Параметры:**
- `selector` (string) - CSS селектор
- `context` (HTMLElement, optional) - контекст поиска (по умолчанию document)

**Возвращает:** HTMLElement | null - найденный элемент или null

**Назначение:** Безопасный поиск элемента с предупреждением в консоль, если элемент не найден.

#### `buildCardDataMap(groups)`
**Параметры:**
- `groups` (Array<Object>) - массив групп с карточками

**Назначение:** Строит маппинг ID карточек на их данные для быстрого доступа при обработке событий.

#### `getCardDataById(id)`
**Параметры:**
- `id` (string) - ID карточки

**Возвращает:** Object | undefined - данные карточки или undefined

**Назначение:** Получает данные карточки по её ID из маппинга.

### HTML Элементы и их ID

**Модальные окна (унифицированные):**
- `permission-modal` - модальное окно декодера permission
- `role-modal` - модальное окно проверки роли
- Папка `JS/` - DevTools-скрипты; имя файла задаётся в JSON карточки (`scriptFile`), копируются в буфер по клику

**Другие элементы:**
- `groups-container` - контейнер для динамически загружаемых групп ресурсов
- `link-info-tooltip` - подсказка с полным URL и разбором ссылки (домен, путь, параметры) при наведении на карточку
- `script-copy-toast` - уведомление об успешном копировании скрипта (источник: файл / встроенная копия, число символов)
- `progress-bar` - индикатор прогресса проверки ссылок
- `export-links-btn-link` - кнопка экспорта ссылок в JSON

**CSS классы для модальных окон:**
- `modal-content` - базовый класс для всех модальных окон (стандартный размер: 700px)
- `modal-buttons-primary` - группа основных кнопок (слева)
- `modal-buttons-secondary` - группа служебных кнопок (справа)

## История версий (Version Control)

### Версия 2.4.17 (Текущая)
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Исправление duplicate 0-границы** — в режиме `Только положительные` устранён артефакт интервала `0..0` после `Равно по ТН`/`Лесенка`
- ✅ **Квантили positive-режима** — при наличии положительных значений расчёт внутренних границ выполняется по `> 0`, чтобы нули не «съедали» первый положительный интервал
- ✅ **Защита от нулевого дубля** — добавлена пост-проверка границ: если после якоря `0` следующая граница тоже `0` и диапазон допускает, она сдвигается вправо

### Версия 2.4.16
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Positive-only границы** — в авторасчёте первый интервал фиксирован как отрицательная зона `[min..0)`, и не участвует в выравнивании
- ✅ **Авторасчёт по положительной части** — `Равно по ТН`, `Лесенка`, `Равные интервалы` в positive-режиме считаются только на диапазоне `0..max`
- ✅ **Сегмент `Авторасчёт границ`** — исправлена визуальная синхронизация состояния переключателя (`Только положительные` / `Все значения`)
- ✅ **Аналитика UI** — графики ограничены максимум 2 в ряд; улучшена читабельность подписей и стабильность подсветки сегментов метрики/topN

### Версия 2.4.15
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Вкладка аналитики расширена** — отдельные переключатели: разрез (`ТБ/ГОСБ/кластер`), метрика (`ТН/Σ/средняя`), размер top/bottom (`5/10/20`)
- ✅ **Набор графиков увеличен до 10** — интервалы, CDF, топы по ТБ/ГОСБ/кластеру, децили, top/bottom по выбранному разрезу, круговая структура, диапазон `P10/P50/P90`
- ✅ **Набор таблиц увеличен до 6** — интервальный профиль, концентрация категорий, процентили и коэффициенты, top/bottom по всем разрезам, матрица `ТБ × кластер`, таблица стабильности
- ✅ **Интерпретация аналитики документирована** — добавлены правила чтения CDF, percentiles/IQR, концентрации и стабильности категорий

**Как интерпретировать аналитику (кратко):**
- `CDF` — используйте для выбора границ по целевым долям ТН (например, где достигается 50%/80%/95%)
- `P50/P90/P99` — показывает «центр» и «хвост» распределения; рост хвоста при стабильной медиане = усиление выбросов
- `IQR` и `Размах` — индикаторы неоднородности; рост IQR обычно требует более детального интервального деления
- `Top/Bottom` по разрезам — помогает выявлять зоны концентрации и «провалы» по ТБ/ГОСБ/кластерам
- `Стабильность` (CV) — большие значения CV сигнализируют о высокой волатильности категории

### Версия 2.4.14
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Новая вкладка** — добавлен режим `Статистика и анализ` для детального анализа текущего файла
- ✅ **6 графиков в аналитике** — интервалы, кумулятивная доля, топ ТБ, топ ГОСБ, топ кластеров, децильный профиль сумм
- ✅ **2 таблицы в аналитике** — интервальный профиль (`ТН/%/средняя`) и концентрация категорий (`ТБ/ГОСБ/кластер`)
- ✅ **Подсказки по выбору границ** — добавлен guidance-блок и tooltip-подсказки для аналитических графиков/таблиц
- ✅ **Интерактив с текущими настройками** — аналитика пересчитывается от текущих фильтров и границ интервалов

### Версия 2.4.13
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Заголовок блока** — удалён поясняющий текст «Обязательные столбцы…» под `Распределение сумм`
- ✅ **Переключатели режимов** — блок `Один период / Сравнение периодов` закреплён сверху; контент прокручивается под ним
- ✅ **Сворачиваемые секции** — блоки `1. Данные` и `2. Фильтры` переведены в `details` как у подсказки формата
- ✅ **Состояния по умолчанию** — `Данные` открыты, `Фильтры` свернуты при загрузке
- ✅ **Структура панели** — вложенные элементы и действия внутри блоков сохранены без изменения логики

### Версия 2.4.12
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Фильтры ТБ/ГОСБ** — дерево переведено на 3 колонки (адаптивно 2/1 по ширине), длинные названия переносятся по строкам
- ✅ **Основной бегунок** — номер группы внутри круга; выбранная для тонкой настройки граница визуально выделяется
- ✅ **Точная настройка границ** — всегда доступна для выбранной границы; переключение между границами и синхрон движения с основной шкалой
- ✅ **Подпись значения** — под выбранным бегунком на основной шкале показывается текущее значение границы

### Версия 2.4.11
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Сравнение (наложение / перекрытие)** — порядок периодов выровнен: слева прошлый, справа текущий
- ✅ **Перекрытие столбиков** — уменьшена глубина перекрытия (визуально чище при близких значениях)
- ✅ **Тонкая настройка границ** — автопоявляющаяся увеличенная шкала для близко расположенных бегунков
- ✅ **Линия в сравнении** — исправлены наложение/рядом, общая шкала и корректный цвет периодов

### Версия 2.4.10
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Фильтры в сравнении** — уникальные ТБ / ГОСБ / кластер из **обоих** периодов (значение только в одном файле всё равно в списке)
- ✅ **Поля границ** под бегунками — равные колонки на всю ширину, по центру; целые с разделителем разрядов
- ✅ **Экспорт в сравнении** — оба периода в одном файле (колонка «Период» / секции текущего и прошлого)
- ✅ **Скачать результаты** — панель внизу с иконками; убран нижний статус «Интервалов… · bars»

### Версия 2.4.9
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Панель графика** — разрез / группы / вид / компоновка над canvas, ровная сетка ячеек
- ✅ **Сегментные переключатели** — вид (гистограмма ↔ линия) и группы (интервалы ↔ значения)
- ✅ **Ограничения режимов** — линия и «наложение с перекрытием» взаимно недоступны; «Значения» — только при разрезе ТБ/кластер
- ✅ **Кнопки границ** — иконки и цвет (равно / лесенка / равные интервалы); компактное поле «Границ»

### Версия 2.4.8
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **Вкладки** — «Один период» / «Сравнение периодов» (**текущий** и **прошлый**)
- ✅ **Виды графика** — гистограмма / линия (площадь убрана; линия — по центрам интервалов)
- ✅ **Разрезы** — вся выборка / ТБ / кластер (ГОСБ и ТБ+ГОСБ убраны)
- ✅ **Сравнение** — наложение / **наложение с перекрытием** (полупрозрачные столбики) / рядом / друг под другом; сводная статистика и таблица Δ
- ✅ **Layout** — данные сверху, график на всю ширину, бегунки снизу; два графика — один под другим
- ✅ **Фильтры** — фиксированный порядок; невыбранные серые; цвет блока (все / частично / ничего); **Выбрать всё / Снять всё** с иконками на ТБ+ГОСБ+кластеры
- ✅ **Группы на графике** работают и в сравнении (в т.ч. в наложении)
- ✅ **Панель файла** вместо вставки CSV — карточки с иконками
- ✅ «Равно» / «Лесенка» считают границы по **текущему** периоду
- ✅ Демо прошлого периода: `tools/build_sum_demo_csv_21k.py` → ~21 000 строк / 1 900 ТН

### Версия 2.4.7
**Дата обновления:** 2026-08-11

**Основные изменения:**
- ✅ **«Равно по числу ТН»** — границы по квантилям числа участников (N ползунков → N+1 интервалов ≈ поровну по всей выборке; внутри ТБ/ГОСБ/кластера доли могут отличаться)
- ✅ **«Лесенка по числу ТН»** — убывающие доли K : K−1 : … : 1 (первый интервал больше всех)
- ✅ **«Равные интервалы»** — равная ширина по шкале сумм (мин…макс делится поровну)
- ✅ Убран выбор режима границ — всегда ползунки и кнопки под графиком
- ✅ На правом (макс) бегунке бейдж номера группы не показывается

### Версия 2.4.6
**Дата обновления:** 2026-08-10

**Основные изменения:**
- ✅ **Дерево фильтров ТБ→ГОСБ** — активные сверху, серые кликабельны; выбор ГОСБ тянет ТБ; выбор ТБ — все его ГОСБ; кластеры отдельно
- ✅ **% в таблице интервалов** — `% всех` и `% в ТБ/ГОСБ/кластере` по разрезу

### Версия 2.4.5
**Дата обновления:** 2026-08-10

**Основные изменения:**
- ✅ **Подписи на столбиках** — число ТН (−90° / над столбиком), цвет по заливке серии; имя `N (ТБ: x / ГОСБ: y)`
- ✅ **Ось X** — адаптивные подписи (stack / tilt / vert) без обрезки за край; в режиме «Значения» — имя ТБ + стата в две вертикальные строки
- ✅ **Hover по столбику** — подсказка: интервал, серия, ТН / ТБ / ГОСБ / кластер
- ✅ **Таблица интервалов** — абсолюты + строка «Итого» (без «попало / всего»)
- ✅ **Группы на графике** — при выборе разреза по умолчанию «Значения разреза»

### Версия 2.4.4
**Дата обновления:** 2026-08-10

**Основные изменения:**
- ✅ **Подробная статистика по интервалам** — ТБ / ГОСБ / кластер / ТН; при разрезе — отдельный блок на каждое значение
- ✅ **Бейджи групп** на ползунках границ (номер «Группа N»)
- ✅ **По умолчанию** — режим ползунков, 2 подвижные границы
- ✅ **Целые границы** (`floor`/`ceil`), формат сумм с разрядами (≤2 знака); мин/макс в сводке без округления шкалы
- ✅ **Единый HTML** — настройки в `#app-config` (JSON); `config.json` удалён

### Версия 2.4.3
**Дата обновления:** 2026-08-10

**Основные изменения:**
- ✅ **Единый HTML** — настройки только в `#app-config` (JSON в начале файла); отдельный `config.json` удалён
- ✅ В конфиг вынесены цвета серий, длина ТН при экспорте, лимиты ползунков/таблицы и др.

### Версия 2.4.2
**Дата обновления:** 2026-08-10

**Основные изменения:**
- ✅ **Целые границы** — мин шкалы `floor`, макс `ceil`; ползунок и ввод только целыми
- ✅ **Формат сумм** — разделитель разрядов (`ru-RU`), не более 2 знаков после запятой (интервалы, среднее, сумма)
- ✅ **Статистика мин/макс** — точные значения из данных, без округления шкалы

### Версия 2.4.1
**Дата обновления:** 2026-08-10

**Основные изменения:**
- ✅ **Легенда** сразу под гистограммой (цвета ТБ / ГОСБ / кластер / интервалов)
- ✅ **Ручные границы** — числовые поля у ползунков: ввод значения сдвигает бегунок (с каскадом порядка)
- ✅ **Экспорт:** CSV исходный + «Группа»; CSV уникальных ТН (8 знаков) + группа; TXT групп в формате `"Группа N": {"тн",…}`

### Версия 2.4.0
**Дата обновления:** 2026-08-10

**Основные изменения:**
- ✅ **`sum-distribution.html` v2** — обязательные колонки ТН + сумма; опциональные ТБ / ГОСБ / кластер; алиасы без учёта регистра
- ✅ **Агрегация по ТН** — `SUM(сумма)` после нормализации ТН до 20 цифр; на гистограмму идут уникальные ТН
- ✅ **Ручные границы** — число подвижных ползунков на одной шкале (`min — ●…● — max`), каскад порядка
- ✅ **UI Liquid Glass** — стили как у ресурсной панели; график шире настроек; полупрозрачные tooltip
- ✅ **Демо-CSV** — генераторы в `tools/` → `samples/` (каталог в `.gitignore`, не в репозитории)
- ✅ **`.gitignore`** — каталоги `IN/` и `JS/IN/` не попадают в репозиторий

### Версия 2.3.2
**Дата обновления:** 2025-06-10

**Основные изменения:**
- ✅ **SUP_Config_Update.js** — доработка скрипта обновления параметров СУП; встроенные копии в `index.html` и POST синхронизированы

### Версия 2.3.1
**Дата обновления:** 2025-06-10

**Основные изменения:**
- ✅ **SUP Config Update** — кнопка и скрипт `SUP_Config_Update.js` (обновление параметров СУП / UFS Config Manager)
- ✅ **`embed_scripts.py`** — список встраиваемых файлов берётся из полей `scriptFile` в JSON конфигурации

### Версия 2.3.0
**Дата обновления:** 2025-06-10

**Основные изменения:**
- ✅ **Рефакторинг группы «Администрирование»** — декодер permission, проверка роли (ПСИ), экспорт ссылок; 9 кнопок копирования DevTools-скриптов (`scriptFile`)
- ✅ **Копирование скриптов без HTTP-сервера** — встроенные копии в `index.html` (маркеры `EMBEDDED_SCRIPTS_START/END`); синхронизация из `JS/` через `python3 tools/embed_scripts.py`
- ✅ **Гибридная загрузка** — сначала файл `JS/`, при ошибке встроенная копия; на `file://` sync-копирование в обработчике клика (исправлен пустой буфер)
- ✅ **Toast источника копирования** — синий: из файла, оранжевый: из HTML; отображается число символов
- ✅ **Каталог POST** — актуальные `.txt`-копии; утилита `python3 tools/sync_post.py`
- ✅ **Тесты** — `src/Tests/test_clipboard*.mjs` (Playwright, проверка буфера на `file://`)

**Решенные проблемы:**
- Страница не загружалась из‑за литерала `</script>` в комментарии основного JS
- Toast «скопировано», но вставка пустая — потеря user activation при async fetch на `file://`

### Версия 2.2.0
**Дата обновления:** 2025-02-09

**Основные изменения:**
- ✅ **Подсказка по ссылке при наведении** — при наведении на карточку появляется полупрозрачное окно (Liquid Glass) с полным URL, доменом, путём и параметрами; путь и параметры выводятся только при наличии (корневой путь `/` не показывается); для карточек с `isModal: true` или `url: "#"` показывается сообщение «Модальное окно или ссылка не задана»
- ✅ **Копирование ссылки по Ctrl/Cmd + клик** — комбинация Ctrl+клик (Windows/Linux) или Cmd+клик (macOS) копирует полный URL карточки в буфер обмена без перехода, с краткой анимацией (пульс по контуру кнопки)
- ✅ **Исправлена модалка «Выгрузка профилей по табельным»** — добавлены обработчики для всех кнопок (ALPHA, SIGMA, Копировать, По умолчанию, Очистить, Закрыть) и генерация скрипта по табельным номерам

**Решенные проблемы:**
- Кнопки в модальном окне «Профили по табельным» не срабатывали — добавлена полная логика и привязка обработчиков
- Ссылки с якорем в конце (`url: "https://...?#"`) ошибочно считались модальными — учёт только `isModal` и строго `url: "#"`
- Документация приведена в соответствие с новым функционалом

### Версия 2.1.0
**Дата обновления:** 2025-01-15

**Основные изменения:**
- ✅ **Полностью переработана система адаптации размеров** - кнопки теперь заполняют весь блок группы
  - Автоматическое вычисление оптимальной высоты кнопок на основе доступного пространства
  - Адаптация размера шрифта под размер кнопок (максимум 18px, минимум 10px)
  - Автоматическое переключение между 1 и 2 колонками (порог: 6 кнопок)
  - Умное уменьшение размеров при необходимости для избежания прокрутки
- ✅ **Экспорт конфигурации в JSON** - добавлена функция экспорта текущей конфигурации в JSON-файл с датой и временем в имени
- ✅ **Улучшен дизайн в стиле Liquid Glass (iOS 26)** - светлая тема с эффектами стекла
- ✅ **Унифицированы все кнопки и модалки** - единый стиль, размеры и поведение
- ✅ **Оптимизирован размер текста** - ограничение до 2 строк, адаптивный размер шрифта

**Решенные проблемы:**
- Устранено разряженное пространство внизу групп
- Кнопки теперь равномерно заполняют весь блок группы
- Шрифт автоматически адаптируется под размер кнопок
- Улучшена читаемость и визуальная согласованность

### Версия 2.0.0
**Дата обновления:** 2025-01-XX

**Основные изменения:**
- ✅ **Рефакторинг на динамическую загрузку контента** - все группы и карточки теперь загружаются из встроенного JSON-конфигурации
- ✅ **Унификация модальных окон** - все модальные окна приведены к единому виду:
  - Единые размеры (700px)
  - Единая структура (заголовок → поля → кнопки → результат → подсказка)
  - Единый порядок кнопок (основные слева, служебные справа)
  - Единые стили без inline-атрибутов
  - Единые медиа-запросы для адаптивности
- ✅ **Улучшение структуры кода**:
  - Добавлен `ModalManager` для централизованного управления модальными окнами
  - Добавлен `Logger` для логирования с уровнями детализации
  - Добавлена утилита `safeQuerySelector` для безопасного поиска элементов
  - Реализована функция `copyToClipboard` с поддержкой SberBrowser
- ✅ **Оптимизация адаптивности** - изменена сетка на 4 колонки по умолчанию с адаптацией до 1 колонки на мобильных
- ✅ **Реорганизация контента** - переносы карточек между разделами, удаление дубликатов

**Решенные проблемы:**
- Устранены дубликаты функций и ID
- Убраны все inline стили из модальных окон
- Исправлены проблемы с CORS при работе без сервера (использование встроенного JSON)
- Оптимизирована производительность рендеринга динамического контента
- Улучшена читаемость и поддерживаемость кода

**Технические детали:**
- Проект использует чистый HTML/CSS/JavaScript без внешних зависимостей
- Конфигурация хранится в виде встроенной JSON-строки в HTML (работает без сервера)
- Все стили инкапсулированы в `<style>` блоке с использованием CSS-переменных
- JavaScript код разделен на логические модули (утилиты, менеджеры, рендеринг)
- Применены современные CSS-технологии (Grid, Flexbox, Custom Properties, Media Queries)

### Версия 1.0.0
**Дата создания:** 2025-01-XX

**Реализованный функционал:**
- ✅ Создана базовая структура HTML-страницы с адаптивным дизайном
- ✅ Реализована система группировки ресурсов по категориям
- ✅ Добавлена автоматическая проверка доступности ссылок
- ✅ Реализованы модальные окна для административных утилит
- ✅ Реализован экспорт всех ссылок в CSV-формат
- ✅ Добавлена функциональность копирования учетных данных в буфер обмена
- ✅ Настроена адаптивная сетка для различных размеров экранов
- ✅ Применена цветовая схема в стиле корпоративного дизайна

**Решенные проблемы:**
- Проблема с проверкой доступности ссылок через CORS решена использованием режима 'no-cors'
- Оптимизирована производительность проверки ссылок с добавлением задержек между запросами
- Реализована корректная обработка ошибок при генерации скриптов
- Добавлена валидация входных данных для всех генераторов скриптов

## Структура проекта

```
RESURCE_PANEL_HTML_WORK/
├── index.html                    # Ресурсная панель (HTML+CSS+JS)
├── sum-distribution.html         # Гистограмма сумм + JSON-конфиг #app-config в начале файла
├── button-types-preview.html     # Превью типов кнопок
├── design-preview.html           # Превью дизайна
├── ROADMAP.md                    # Roadmap страницы распределения сумм
├── README.md                     # Документация проекта
├── .gitignore                    # В т.ч. IN/, JS/IN/, samples/, log/, .env
├── samples/                      # Демо-CSV (не в git; генерируются tools/)
├── tools/
│   ├── embed_scripts.py          # JS/ → встроенные блоки в index.html
│   ├── sync_post.py              # index.html + JS/ → POST/*.txt
│   ├── build_sum_demo_csv.py     # Демо 1800 из локальных IN/
│   └── build_sum_demo_csv_20k.py # Демо 20000 / 2000 ТН (синтетика)
├── src/Tests/                    # Тесты (гистограмма, clipboard/Playwright)
├── Docs/
│   └── РЕКОМЕНДАЦИИ_ПО_УЛУЧШЕНИЮ.md
├── JS/                           # DevTools-скрипты (редактирование)
│   ├── AddressBook_export_OE.js
│   ├── … 
│   └── SUP_Config_Update.js
├── POST/                         # Копии *.txt для обмена
│   ├── index.html.txt
│   └── JS/*.js.txt
└── IN/                           # Сырые выгрузки (локально, не в git)
```

**Особенности структуры:**
- Основная панель — один файл `index.html`; распределение сумм — отдельный `sum-distribution.html`
- Конфигурация групп панели встроена в HTML как JSON-строка
- Документация и рекомендации хранятся в папке `Docs`
- В каталоге `POST` — копии кода с расширением `.txt`. Обновление: `python3 tools/sync_post.py`
- DevTools-скрипты редактируются в `JS/`; встраивание: `python3 tools/embed_scripts.py` (для `file://`)
- Каталоги `IN/` и `JS/IN/` в `.gitignore` — персональные/сырые данные не коммитятся

## Использование

1. **Откройте `index.html`** в браузере двойным щелчком (`file://`) — HTTP-сервер и Python не нужны
2. **Используйте карточки** для быстрого доступа к ресурсам - они автоматически загружаются из конфигурации
3. **Для административных задач** используйте группу "Администрирование":
   - Декодер permission, проверка роли (ПСИ)
   - Кнопки DevTools-скриптов (`scriptFile`) — клик копирует текст в буфер; toast показывает источник (файл `JS/` или встроенная копия) и длину
   - Экспорт ссылок в JSON
4. **Подсказка по ссылке** — при наведении на карточку отображается полный URL, домен, путь и параметры в полупрозрачном окне в стиле Liquid Glass
5. **Копирование ссылки** — Ctrl+клик (или Cmd+клик на Mac) по карточке копирует её URL в буфер обмена без перехода по ссылке (не для карточек со `scriptFile`)
6. **Скрипты в DevTools** — вставьте из буфера (Ctrl+V) в консоль на нужной странице
7. **Экспорт конфигурации** - используйте кнопку "Экспорт ссылок" для сохранения текущей конфигурации в JSON-файл с датой и временем
8. **Распределение сумм** — откройте `sum-distribution.html`, загрузите CSV (или `samples/sum-distribution-demo-1800.csv`)

### Страница распределения сумм (`sum-distribution.html`)

Однофайловый инструмент (HTML + CSS + JS, стиль Liquid Glass) для анализа распределения сумм по интервалам.

**Обязательные столбцы:** `ТН`, `сумма`. **Опциональные:** `ТБ`, `ГОСБ`, `кластер` (без них нет фильтров/разрезов по полю). Регистр заголовков не важен; поддерживаются алиасы (например сумма/прирост/итог, ТН/TN/табельный).

**Агрегация:** перед графиком строки группируются по нормализованному ТН (20 цифр с ведущими нулями), значение = `SUM(сумма)` по ТН. На интервалы идёт одна точка на ТН. Ось Y гистограммы — **число ТН** в интервале.

**Алиасы (приоритет — раньше в списке):**
- сумма: сумма, прирост, прирос, рост, итог, sum, amount
- ТН: тн, tn, табельный, таб.ном, табельный номер
- ТБ: тб, tb, тербанк · ГОСБ: госб, gosb · кластер: кластер, cluster

**Возможности:**
- вкладки **Один период** / **Сравнение** (текущий + прошлый CSV);
- над графиком: ровная панель **вид** / **компоновка** / **разрез** / **группы** (сегментные переключатели для вида и групп);
- виды: **гистограмма** / **линия** (линия недоступна при наложении с перекрытием и наоборот);
- фильтры: дерево **ТБ→ГОСБ** (порядок фиксированный, невыбранные серые); в широком окне — 3 колонки (адаптивно 2/1), длинные названия переносятся; кластеры ниже по центру; цвет блока по состоянию выбора; **Выбрать всё / Снять всё** сразу для ТБ+ГОСБ+кластеров; в сравнении — **уникальные значения из обоих периодов**;
- разрез: вся выборка / ТБ / кластер;
- **группы на графике**: «Интервалы» ↔ «Значения» (значения — только при разрезе ТБ/кластер);
- сравнение: наложение / **наложение с перекрытием** / рядом / друг под другом; общие границы и фильтры; для overlay периоды идут слева направо: прошлый → текущий;
- интервалы: только **ручные границы** (ползунки под графиком) — **целые**; кнопки с иконками **«Равные интервалы»**, **«Равно по ТН»**, **«Лесенка»** (в сравнении — по текущему периоду); компактное поле «Границ»; поля мин / группа N / макс — **равные колонки на ширину шкалы**, числа с разделителем разрядов без дробей;
- под основной шкалой есть блок **«Точная настройка»** для выбранной границы (диапазон соседних границ, шаг 1, переключение между границами, синхрон с основной шкалой);
- суммы в подписях/среднем/сумме — с разделителем разрядов, ≤2 знака после запятой; **мин/макс в блоке статистики** — точные значения данных;
- **легенда** с цветами сразу под canvas;
- гистограмма (Canvas) + подписи **числа ТН**; адаптивные подписи оси X; **hover**;
- **детальная статистика по интервалам**: ТБ / ГОСБ / кластер / ТН, **%**, строка **Итого**; при сравнении — таблица Δ;
- **сохранение результатов** (панель внизу с иконками; группа = «Группа 1…N» по интервалу суммы ТН):
  - CSV исходных строк + колонка `Группа` (в сравнении + колонка `Период`, оба файла);
  - CSV уникальных ТН (**8 знаков** с ведущими нулями) + `Группа` (в сравнении — оба периода);
  - TXT: группы → списки ТН (в сравнении — секции текущего и прошлого);
- полупрозрачные подсказки при наведении на элементы UI;
- layout: данные сверху → график на всю ширину → бегунки → статистика → **скачать результаты**.

**Демо-данные** (каталог `samples/` в `.gitignore`, в репозиторий не коммитить):

| Файл | Как получить |
|------|----------------|
| `samples/sum-distribution-demo-20000.csv` | `python3 tools/build_sum_demo_csv_20k.py` — 20 000 строк, 2 000 уник. ТН (текущий период) |
| `samples/sum-distribution-demo-21000.csv` | `python3 tools/build_sum_demo_csv_21k.py` — ~21 000 строк, 1 900 уник. ТН (прошлый период) |
| `samples/sum-distribution-demo-1800.csv` | `python3 tools/build_sum_demo_csv.py` — из локальных `IN/` (если есть) |

Пересборка демо:

```bash
python3 tools/build_sum_demo_csv_20k.py
python3 tools/build_sum_demo_csv_21k.py
```

```bash
python3 tools/build_sum_demo_csv.py
```

**Параметры:** блок `<script type="application/json" id="app-config">` в начале `sum-distribution.html` (отдельный `config.json` не используется). Описание roadmap: `ROADMAP.md`.

**Тесты:**
```bash
node src/Tests/test_histogram_bins.js
```

Каталоги `IN/` и `JS/IN/` в `.gitignore` — сырые выгрузки в репозиторий не попадают.

**Обновление скриптов после правок в `JS/` (для разработчиков):**
```bash
python3 tools/embed_scripts.py   # встроить актуальные JS/ в index.html
python3 tools/sync_post.py       # обновить POST/*.txt
```

**Готовый `index.html` для пользователей:** достаточно одного файла — скрипты уже вшиты между `EMBEDDED_SCRIPTS_START` и `EMBEDDED_SCRIPTS_END`. Папка `JS/` рядом не обязательна.

**Добавление нового скрипта:**
1. Положить `.js` в `JS/`
2. Добавить карточку в JSON группы «Администрирование» с полем `"scriptFile": "Имя.js"`
3. Выполнить `python3 tools/embed_scripts.py` и `python3 tools/sync_post.py`

**Изменение контента:**
Для добавления новых групп или карточек отредактируйте встроенный JSON в функции `loadAndRenderConfig()` в файле `index.html`. Структура JSON:
```json
{
  "groups": [
    {
      "id": 1,
      "name": "Название группы",
      "code": "код_для_копирования",
      "cards": [
        {
          "title": "Название карточки",
          "url": "https://example.com",
          "id": "id_кнопки_для_копирования",
          "copyCode": "код_для_копирования",
          "scriptFile": "ИмяФайла.js",
          "isModal": false
        }
      ]
    }
  ]
}
```

## Совместимость с браузерами и ОС

Приложение рассчитано на работу в **Windows**, **macOS**, **Linux и ОС на базе Linux** (в т.ч. **SberOS**):

- **SberBrowser** (Windows, SberOS/Linux) — поддерживается; при проблемах на SberOS проверьте, что используется актуальная версия браузера.
- **Chrome, Firefox, Edge, Safari** — рекомендуются современные версии.

**Реализованные меры совместимости:**

- **CSS:** при отсутствии поддержки `backdrop-filter` (часть старых сборок WebKit/Chromium на Linux) интерфейс автоматически переключается на фоны без размытия — страница остаётся рабочей и читаемой.
- **JavaScript:** подключён полифилл для `String.prototype.padStart` (для старых движков); при ошибке инициализации выводится сообщение с предложением обновить страницу или браузер.
- **Копирование в буфер:** Clipboard API с fallback через событие `copy` и `execCommand`; на `file://` скрипты копируются синхронно в обработчике клика
- **Встроенные скрипты:** блоки `embedded-script` в `index.html` — запасной источник при недоступности `JS/` по `file://`

Если страница не открывается (пустой экран) на SberOS или другом Linux-браузере, обновите браузер до последней версии или откройте страницу в Chrome/Firefox под той же ОС.

## Технические требования

- Браузер с поддержкой ES5+ (рекомендуется ES6+): Chrome, Firefox, Safari, Edge, SberBrowser
- JavaScript должен быть включён
- Для работы части функций требуется доступ к внутренним сервисам организации

## Примечания

- Все ссылки открываются в новых вкладках (`target="_blank"`)
- Проверка доступности ссылок выполняется автоматически при загрузке и каждые 2 минуты
- Некоторые функции требуют авторизации в соответствующих системах
- Сгенерированные скрипты используют cookie текущей сессии браузера
- Кнопки автоматически адаптируются под размер блока группы для оптимального заполнения пространства
